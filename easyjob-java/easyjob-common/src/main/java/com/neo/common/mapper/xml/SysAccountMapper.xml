<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neo.common.mapper.SysAccountMapper">

    <select id="selectAccountTest" resultType="com.neo.common.entity.po.SysAccount">
        select *
        from sys_account
        where user_id = #{item}
    </select>
</mapper>
